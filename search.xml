<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>curl 检测代理的可用性以及延迟</title>
      <link href="/2023/02/20/curl%20%E6%A3%80%E6%B5%8B%E4%BB%A3%E7%90%86%E7%9A%84%E5%8F%AF%E7%94%A8%E6%80%A7%E4%BB%A5%E5%8F%8A%E5%BB%B6%E8%BF%9F/"/>
      <url>/2023/02/20/curl%20%E6%A3%80%E6%B5%8B%E4%BB%A3%E7%90%86%E7%9A%84%E5%8F%AF%E7%94%A8%E6%80%A7%E4%BB%A5%E5%8F%8A%E5%BB%B6%E8%BF%9F/</url>
      
        <content type="html"><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>在办公网的代理翻墙的过程中，经常没办法第一时间知道代理失效了，因为我们自身不是高用的用户，每次挂了都需要员工来反馈，体感非常的不好，因此想着可以通过<code>zabbix</code>如果把当前的延迟、可用性检测起来</p><h1 id="通过Curl-检测Google的延迟"><a href="#通过Curl-检测Google的延迟" class="headerlink" title="通过Curl 检测Google的延迟"></a>通过Curl 检测Google的延迟</h1><p>这里为啥是curl而不是ping，因为默认ping事不支持代理的，然而curl也可以做到真正的是否可用，有延迟就一定说明是可用的</p><pre><code class="bash">curl -o /dev/null -x socks5h://127.0.0.1:12126 -s --connect-timeout 5 -w %&#123;time_starttransfer&#125;&quot;\n&quot; $1</code></pre><p><code>socks5h</code>的地址需要改成你的代理地址</p><h1 id="获取当前代理的可用性"><a href="#获取当前代理的可用性" class="headerlink" title="获取当前代理的可用性"></a>获取当前代理的可用性</h1><pre><code class="bash">url=$1result=($(curl -x socks5h://localhost:12126 -I -s --connect-timeout 5 $&#123;url&#125; | head -1 | tr &quot;\r&quot; &quot;\n&quot;))if [ &quot;$&#123;result[1]&#125;&quot; == $2 -a &quot;$&#123;result[2]&#125;&quot; == &#39;OK&#39; ]then  echo 1else  echo 0fi</code></pre><h1 id="zabbix操作脚本"><a href="#zabbix操作脚本" class="headerlink" title="zabbix操作脚本"></a>zabbix操作脚本</h1><pre><code class="bash"># 延迟UserParameter=googleTime[*],/etc/zabbix/script/googleTime.sh $1# 可用UserParameter=googleCheck[*],/etc/zabbix/script/googleCheck.sh $1 $2</code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> curl </tag>
            
            <tag> 代理 </tag>
            
            <tag> 检测 </tag>
            
            <tag> zabbix </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>记录一次办公网全球化的改造计划</title>
      <link href="/2023/02/17/%E8%AE%B0%E5%BD%95%E4%B8%80%E6%AC%A1%E5%8A%9E%E5%85%AC%E7%BD%91Panabit%E5%85%A8%E7%90%83%E5%8C%96%E7%9A%84%E6%94%B9%E9%80%A0%E8%AE%A1%E5%88%92/"/>
      <url>/2023/02/17/%E8%AE%B0%E5%BD%95%E4%B8%80%E6%AC%A1%E5%8A%9E%E5%85%AC%E7%BD%91Panabit%E5%85%A8%E7%90%83%E5%8C%96%E7%9A%84%E6%94%B9%E9%80%A0%E8%AE%A1%E5%88%92/</url>
      
        <content type="html"><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>办公网每次去海外找资料都需要重新连接VPN，或者自己连接自己买的小飞机之类的才可以。但是这种在互联网公司内的话，非常的不友好；为公司工作还要自己花钱买小飞机～</p><p>之前尝试过下面这种方式：<br>新增一台海外的机器（新加坡、香港）搭建SS&#x2F;v2ray&#x2F;trojan之类的协议，然后办公网新增一个软路由去连接，通过ACL把部分IP的用户跳转过去；<br>但是这种方式自己家里用用还行，如果想要在企业的办公网来使用的话，人一多就不行，因为所有人都从这个IP出去了，而且每次都需要命令行去操作ACL增加用户，非常的麻烦；</p><h1 id="名词介绍"><a href="#名词介绍" class="headerlink" title="名词介绍"></a>名词介绍</h1><table><thead><tr><th>名词</th><th>介绍</th></tr></thead><tbody><tr><td>Panabit</td><td>Panabit是国内X86平台单板处理能力最高（双向40G）、在运营商和高校等行业案例过千（X运营商千兆以上规格共计400余台已普遍连续稳定运行至第7年），实时对超过3TB的网络带宽进行DPI识别与优化服务、并针对中小企业提供免费版本（软件形态），是以DPI为核心优势并发展起来的专业、上线效果好、性价比高的新一代应用网关。进入2014年，实际支持国内应用协议超过800种，并已集成路由、负载均衡、认证、一拖N检测、移动终端识别、DNS管控、HTTP管控、日志审计等实用功能于一体。</td></tr></tbody></table><h1 id="Panabit-的-Iwan"><a href="#Panabit-的-Iwan" class="headerlink" title="Panabit 的 Iwan"></a>Panabit 的 Iwan</h1><p>为了解决前面的这种方式，决定测试使用 Panabit 的 Iwan 的方式，也就是所谓的 Panabit 的 <code>SD-WAN</code>；</p><ul><li>重连速度很快：<br>        比L2TP的要快一个数量级，L2TP要重连，需要有几十次交互，而我们只需要一次即可  </li><li>客户端不受底层承载线路IP变化影响：<br>        当底层承载线路（比如PPPOE拨号线路）的IP地址发生变化时，不会影响iWAN隧道，iWAN隧道不会中断，保证通信正常进行；<br>        因为很多用户是通过PPPOE拨号线路出去的，PPPOE拨号线路重拨时一般会改变IP地址，如果用L2TP的话，那么这个L2TP会话就要重建；<br>        而用iWAN的话，现有的会话可以照常使用，不需要做任何改变；  </li><li>传输效率高：<br>        iWAN的包头很小，只有8个字节，而且在后续版本里，我们会压缩IP报文头，这样可以继续减少额外报文头的大小，所以能大幅度提升传输效率；<br>        如果用国际线路的话，节省下来的流量费用都是很可观的；  </li><li>抗干扰：<br>        不像L2TP，中间人可以直接发包TERMINATE，iWAN控制命令有完整性检查，可以避免中间人攻击。</li></ul><h1 id="部署"><a href="#部署" class="headerlink" title="部署"></a>部署</h1><h2 id="1、搭建panabit"><a href="#1、搭建panabit" class="headerlink" title="1、搭建panabit"></a>1、搭建panabit</h2><p>记得服务器申请2H的，1H需要需改核心，非常麻烦<br>下载Linux系统文件：文末<br>上传文件到root根目录下</p><pre><code class="bash">tar -xzf PanabitFREE_SUIr2p3_20220413_Linux3.tar.gzcd PanabitFREE_SUIr2p3_20220413_Linux3# 输入以下命令进行安装./ipeinstall</code></pre><p>修改<code>/etc/PG.conf</code>文件<br>因为是单网口，所以数据口和管理口都需要配置成eth0，后面不要加任何东西</p><pre><code class="bash">DATA_PORTS 修改成：DATA_PORTS=&quot;eth0&quot;</code></pre><h3 id="修改端口"><a href="#修改端口" class="headerlink" title="修改端口"></a>修改端口</h3><h4 id="上传文件，修改配置"><a href="#上传文件，修改配置" class="headerlink" title="上传文件，修改配置"></a>上传文件，修改配置</h4><p>需要上传一个joskmc文件（文件在文末）<br>在<code>/etc/PG.conf</code>中新增一下一行</p><pre><code class="bash">HTTPS_PORT=2194</code></pre><h4 id="执行joskmc"><a href="#执行joskmc" class="headerlink" title="执行joskmc"></a>执行joskmc</h4><pre><code class="bash">/root/joskmc tcp 2194</code></pre><h4 id="修改：-etc-rc-local，增加一下三行"><a href="#修改：-etc-rc-local，增加一下三行" class="headerlink" title="修改：/etc/rc.local，增加一下三行"></a>修改：<code>/etc/rc.local</code>，增加一下三行</h4><pre><code class="bash">sleep 10/root/joskmc tcp 2194</code></pre><h2 id="2、进行隧道配置（海外）"><a href="#2、进行隧道配置（海外）" class="headerlink" title="2、进行隧道配置（海外）"></a>2、进行隧道配置（海外）</h2><h3 id="1-、登录WEB页面，修改网卡方向"><a href="#1-、登录WEB页面，修改网卡方向" class="headerlink" title="(1)、登录WEB页面，修改网卡方向"></a>(1)、登录WEB页面，修改网卡方向</h3><p>默认账号密码：admin&#x2F;panabit<br>系统概况 → 网络接口：eth0，修改成对外，只有对外才可以创建WAN线路<br><img src="https://raw.githubusercontent.com/almightyYantao/blog-img/master/20230217103308.png"/></p><h3 id="2-、创建WAN线路"><a href="#2-、创建WAN线路" class="headerlink" title="(2)、创建WAN线路"></a>(2)、创建WAN线路</h3><p>应用路由 → 接口线路：<br>需要注意，Mac地址必须克隆<br><img src="https://raw.githubusercontent.com/almightyYantao/blog-img/master/202302171033490.png"/></p><h3 id="3-、创建IWAN连接账号"><a href="#3-、创建IWAN连接账号" class="headerlink" title="(3)、创建IWAN连接账号"></a>(3)、创建IWAN连接账号</h3><p>对象管理 → 账号管理 → 组织架构：<br>地址范围：这一块可以自己定一个内网的IP段就可以，不要冲突就好<br><img src="https://raw.githubusercontent.com/almightyYantao/blog-img/master/20230217103417.png"/><br>地址范围需要把网关地址留出来！！！！！<br><img src="https://raw.githubusercontent.com/almightyYantao/blog-img/master/20230217103432.png"/><br>对象管理 → 账号管理 → 本地账号：<br>处理用户组需要选择前面创建的用户组，其他的根据实际情况填写<br><img src="https://raw.githubusercontent.com/almightyYantao/blog-img/master/20230217103454.png"/></p><h3 id="4-、创建iWAN服务"><a href="#4-、创建iWAN服务" class="headerlink" title="(4)、创建iWAN服务"></a>(4)、创建iWAN服务</h3><p>应用路由 → iWAN服务 → 服务列表：<br>注意：服务器网关地址要和你前面设置的地址范围要在一块，并且需要排除这个地址的下发<br><img src="https://raw.githubusercontent.com/almightyYantao/blog-img/master/20230217103517.png"/><br>应用路由 → iWAN服务 → 服务映射：<br>根据配置情况选择即可<br>iWAN使用的是UDP连接，因此端口需要开放UDP<br><img src="https://raw.githubusercontent.com/almightyYantao/blog-img/master/20230217103531.png"/></p><h3 id="5-、创建策略路由"><a href="#5-、创建策略路由" class="headerlink" title="(5)、创建策略路由"></a>(5)、创建策略路由</h3><p>应用路由 → 策略路由：需要添加一条回程的全程路由，要不然DNS牵引、FQ都会失败<br>这里选择iWAN的线路<br><img src="https://raw.githubusercontent.com/almightyYantao/blog-img/master/20230217103546.png"/></p><h2 id="3、客户端配置（办公网）"><a href="#3、客户端配置（办公网）" class="headerlink" title="3、客户端配置（办公网）"></a>3、客户端配置（办公网）</h2><h3 id="1-、新建WAN线路"><a href="#1-、新建WAN线路" class="headerlink" title="(1)、新建WAN线路"></a>(1)、新建WAN线路</h3><p>应用路由 → 接口线路 → WAN线路：按照信息提示填写即可完成iWAN线路配置<br>注意：必须有一个外网的网卡，并且最好把加密开起来<br><img src="https://raw.githubusercontent.com/almightyYantao/blog-img/master/20230217103622.png"/><br><img src="https://raw.githubusercontent.com/almightyYantao/blog-img/master/20230217103634.png"/></p><h3 id="2-、设置DNS牵引"><a href="#2-、设置DNS牵引" class="headerlink" title="(2)、设置DNS牵引"></a>(2)、设置DNS牵引</h3><p>应用路由 → DNS管控：海外域名是一个域名群组，可以自己修改<br>主要解决DNS污染问题，要不然可能部分网站会无法访问<br>这里有一个很注意的点，就是你的DNS，访问DNS的链路必须经过PA，否则牵引不会生效<br><img src="https://raw.githubusercontent.com/almightyYantao/blog-img/master/20230217103653.png"/></p><h3 id="3-、设置策略路由"><a href="#3-、设置策略路由" class="headerlink" title="(3)、设置策略路由"></a>(3)、设置策略路由</h3><p>应用路由 → 策略路由：我这边直接拿了飞连的609海外分流IP段进行分流，你也可以自己修改（文末下载）<br>主要为了只有需要海外的才出去，不能把所有的流量全部导出去<br><img src="https://raw.githubusercontent.com/almightyYantao/blog-img/master/20230217103713.png"/></p><h1 id="附件"><a href="#附件" class="headerlink" title="附件"></a>附件</h1><p>169IP段： <a href="https://www.123pan.com/s/cRk7Vv-frSsH">https://www.123pan.com/s/cRk7Vv-frSsH</a> 提取码:NzAF<br>Linux操作系统： <a href="https://www.123pan.com/s/cRk7Vv-arSsH">https://www.123pan.com/s/cRk7Vv-arSsH</a> 提取码:A5MC<br>joskmc： <a href="https://www.123pan.com/s/cRk7Vv-BrSsH">https://www.123pan.com/s/cRk7Vv-BrSsH</a> 提取码:kTu9</p>]]></content>
      
      
      
        <tags>
            
            <tag> 办公网 </tag>
            
            <tag> 全球化 </tag>
            
            <tag> 全球办公 </tag>
            
            <tag> 国际化 </tag>
            
            <tag> Panabit </tag>
            
            <tag> iwan </tag>
            
            <tag> sd-wan </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>ss-tproxy 透明代理的设置方法</title>
      <link href="/2023/02/16/ssTproxyTransparentAgent/"/>
      <url>/2023/02/16/ssTproxyTransparentAgent/</url>
      
        <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="一、介绍"><a href="#一、介绍" class="headerlink" title="一、介绍"></a>一、介绍</h1><h2 id="1、什么是透明代理"><a href="#1、什么是透明代理" class="headerlink" title="1、什么是透明代理"></a>1、什么是透明代理</h2><p>在正向代理中，一个软件如果想走 client 的代理服务，我们必须显式配置该软件，对该软件来说，有没有走代理是很明确的，大家都“心知肚明”。而透明代理则与正向代理相反，当我们设置好合适的防火墙规则（仅以 Linux 的 iptables 为例），我们将不再需要显式配置这些软件来让其经过代理或者不经过代理（直连），因为这些软件发出的流量会自动被 iptables 规则所处理，那些我们认为需要代理的流量，会被通过合适的方法发送到 client 进程，而那些我们不需要代理的流量，则直接放行（直连）。这个过程对于我们使用的软件来说是完全透明的，软件自身对其一无所知。这就叫做 <strong>透明代理</strong>。注意，所谓透明是对我们使用的软件透明，而非对 client、server 或目标网站透明，理解这一点非常重要。</p><h2 id="2、透明代理的工作原理"><a href="#2、透明代理的工作原理" class="headerlink" title="2、透明代理的工作原理"></a>2、透明代理的工作原理</h2><p>在正向代理中，期望使用代理的软件会通过 http、socks5 协议与 client 进程进行交互，以此完成代理操作。而在透明代理中，我们的软件发出的流量是完全正常的流量，并没有像正向代理那样，使用 http、socks5 等专用协议，这些流量经过 iptables 规则的处理后，会被通过“合适的方法”发送给 client 进程（当然是指那些我们认为需要走代理的流量）。注意，此时 client 进程接收到不再是 http、socks5 协议数据，而是经过 iptables 处理的“透明代理数据”，“透明代理数据”从本质上来说与正常数据没有区别，只是多了一些“元数据”在里面，使得 client 进程可以通过 netfilter 或操作系统提供的 API 接口来获取这些元数据（元数据其实就是原始目的地址和原始目的端口）。那么这个“合适的方法”是什么？目前来说有两种：</p><ul><li>REDIRECT：只支持 TCP 协议的透明代理。</li><li>TPROXY：支持 TCP 和 UDP 协议的透明代理。</li></ul><p>因此，对于 TCP 透明代理，有两种实现方式，一种是 REDIRECT，一种是 TPROXY；而对于 UDP 透明代理，只能通过 TPROXY 方式来实现。为方便叙述，本文以 <strong>纯 TPROXY 模式</strong> 指代 TCP 和 UDP 都使用 TPROXY 来实现，以 <strong>REDIRECT + TPROXY 模式</strong> 指代 TCP 使用 REDIRECT 实现，而 UDP 使用 TPROXY 来实现，有时候简称 <strong>REDIRECT 模式</strong>，它们都是一个意思。</p><h1 id="二、Openvpn流量转发"><a href="#二、Openvpn流量转发" class="headerlink" title="二、Openvpn流量转发"></a>二、Openvpn流量转发</h1><p>采用：<a href="https://github.com/zfl9/ss-tproxy">https://github.com/zfl9/ss-tproxy</a><br>原理：openvpn → iptables → ss-redir</p><h1 id="三、部署方式"><a href="#三、部署方式" class="headerlink" title="三、部署方式"></a>三、部署方式</h1><h2 id="1、依赖检查"><a href="#1、依赖检查" class="headerlink" title="1、依赖检查"></a>1、依赖检查</h2><p>在部署之前，需要检查下当前自己的服务器依赖是否完整：<br>依赖安装地址：<a href="https://github.com/zfl9/ss-tproxy/wiki/Linux-%E9%80%8F%E6%98%8E%E4%BB%A3%E7%90%86#%E5%AE%89%E8%A3%85%E4%BE%9D%E8%B5%96">https://github.com/zfl9/ss-tproxy/wiki/Linux-%E9%80%8F%E6%98%8E%E4%BB%A3%E7%90%86#%E5%AE%89%E8%A3%85%E4%BE%9D%E8%B5%96</a></p><h2 id="2、脚本安装"><a href="#2、脚本安装" class="headerlink" title="2、脚本安装"></a>2、脚本安装</h2><pre><code class="bash">git clone https:``//github.com/zfl9/ss-tproxycd ss-tproxychmod +x ss-tproxy</code></pre><h3 id="install-可用的情况下（推荐）"><a href="#install-可用的情况下（推荐）" class="headerlink" title="install 可用的情况下（推荐）"></a>install 可用的情况下（推荐）</h3><pre><code class="bash">install ss-tproxy /usr/local/bininstall -d /etc/ss-tproxyinstall -m` `644` `ss-tproxy.conf gfwlist* chnroute* ignlist* /etc/ss-tproxyinstall -m` `644` `ss-tproxy.service /etc/systemd/system # 可选，安装 service 文件</code></pre><h3 id="install-不可用"><a href="#install-不可用" class="headerlink" title="install 不可用"></a>install 不可用</h3><pre><code class="bash">cp -af ss-tproxy /usr/local/binmkdir -p /etc/ss-tproxycp -af ss-tproxy.conf gfwlist* chnroute* ignlist* /etc/ss-tproxycp -af ss-tproxy.service /etc/systemd/system # 可选，安装 service 文件</code></pre><p>配置文件：</p><pre><code class="bash">**/etc/ss-tproxy/ss-tproxy.conf**## mode#mode=&#39;global&#39;  # global 模式 (不分流)mode=&#39;gfwlist&#39; # gfwlist 模式 (黑名单)#mode=&#39;chnroute&#39; # chnroute 模式 (白名单) ## ipv4/6ipv4=&#39;true&#39;     # true:启用ipv4透明代理; false:关闭ipv4透明代理ipv6=&#39;false&#39;    # true:启用ipv6透明代理; false:关闭ipv6透明代理 ## tproxytproxy=&#39;false&#39;  # true:TPROXY+TPROXY; false:REDIRECT+TPROXY ## tcponlytcponly=&#39;false&#39; # true:仅代理TCP流量; false:代理TCP和UDP流量 ## selfonlyselfonly=&#39;false&#39; # true:仅代理本机流量; false:代理本机及&quot;内网&quot;流量 ## proxy# user/group(#1,推荐) vs svraddr+port(#2), user/group选其中一个填写(不建议都填)proxy_procuser=&#39;proxy&#39;   # 本机代理进程的 user/uid，用来放行本机代理进程传出的流量proxy_procgroup=&#39;&#39;       # 本机代理进程的 group/gid，用来放行本机代理进程传出的流量proxy_svraddr4=()        # 服务器的 IPv4 地址或域名，允许填写多个服务器地址，空格隔开proxy_svraddr6=()        # 服务器的 IPv6 地址或域名，允许填写多个服务器地址，空格隔开proxy_svrport=&#39;&#39;         # 服务器的监听端口，可填多个端口，格式同 ipts_proxy_dst_portproxy_tcpport=&#39;12121&#39;    # ss/ssr/v2ray 等本机进程的 TCP 监听端口，该端口支持透明代理proxy_udpport=&#39;12121&#39;    # ss/ssr/v2ray 等本机进程的 UDP 监听端口，该端口支持透明代理proxy_startcmd=&#39;(ss-redir -c /etc/shadowsocks-libev/config.json -u &lt;/dev/null &amp;&gt;&gt;/var/log/ss-redir.log &amp;)&#39;     # 用于启动本机代理进程的 shell 命令，该命令应该能立即执行完毕proxy_stopcmd=&#39;kill -9 $(pidof ss-redir)&#39;      # 用于关闭本机代理进程的 shell 命令，该命令应该能立即执行完毕 ## dnsdns_direct=&#39;223.5.5.5&#39;                # 本地 IPv4 DNS，不能指定端口，也可以填组织、公司内部 DNSdns_direct6=&#39;240C::6666&#39;              # 本地 IPv6 DNS，不能指定端口，也可以填组织、公司内部 DNSdns_remote=&#39;8.8.8.8#53&#39;               # 远程 IPv4 DNS，必须指定端口，提示：访问远程 DNS 会走代理dns_remote6=&#39;2001:4860:4860::8888#53&#39; # 远程 IPv6 DNS，必须指定端口，提示：访问远程 DNS 会走代理 ## dnsmasqdnsmasq_bind_port=&#39;53&#39;                  # dnsmasq 服务器监听端口，见 READMEdnsmasq_cache_size=&#39;4096&#39;               # DNS 缓存大小，大小为 0 表示禁用缓存dnsmasq_cache_time=&#39;3600&#39;               # DNS 缓存时间，单位是秒，最大 3600 秒dnsmasq_query_maxcnt=&#39;1024&#39;             # 设置并发 DNS 查询的最大数量，默认为 150dnsmasq_log_enable=&#39;false&#39;              # 记录详细日志，除非进行调试，否则不建议启用dnsmasq_log_file=&#39;/var/log/dnsmasq.log&#39; # 日志文件，如果不想保存日志可以改为 /dev/nulldnsmasq_conf_dir=()                     # `--conf-dir` 选项的参数，可以填多个，空格隔开dnsmasq_conf_file=()                    # `--conf-file` 选项的参数，可以填多个，空格隔开dnsmasq_conf_string=()                  # 自定义配置，一个数组元素就是一行配置，空格隔开 ## chinadnschinadns_bind_port=&#39;65353&#39;               # chinadns-ng 服务器监听端口，通常不用改动chinadns_timeout=&#39;5&#39;                     # 等待上游 DNS 返回响应的超时时间，单位为秒chinadns_repeat=&#39;1&#39;                      # 向可信 DNS 发送几次 DNS 查询请求，默认为 1chinadns_fairmode=&#39;true&#39;                 # 使用公平模式，具体看 chinadns-ng 的 READMEchinadns_gfwlist_mode=&#39;true&#39;             # gfwlist 模式，加载 gfwlist.txt/gfwlist.extchinadns_noip_as_chnip=&#39;false&#39;           # 启用 chinadns-ng 的 `--noip-as-chnip` 选项chinadns_verbose=&#39;false&#39;                 # 记录详细日志，除非进行调试，否则不建议启用chinadns_logfile=&#39;/var/log/chinadns.log&#39; # 日志文件，如果不想保存日志可以改为 /dev/nullchinadns_privaddr4=()                    # IPv4 私有地址段，多个用空格隔开，具体见 READMEchinadns_privaddr6=()                    # IPv6 私有地址段，多个用空格隔开，具体见 README ## dns2tcpdns2tcp_bind_port=&#39;65454&#39;               # dns2tcp 转发服务器监听端口，如有冲突请修改dns2tcp_tcp_syncnt=&#39;&#39;                   # dns2tcp 的 `-s` 选项，留空表示不设置此选项dns2tcp_tcp_quickack=&#39;false&#39;            # dns2tcp 的 `-a` 选项，选项取值为true/falsedns2tcp_tcp_fastopen=&#39;false&#39;            # dns2tcp 的 `-f` 选项，选项取值为true/falsedns2tcp_verbose=&#39;false&#39;                 # 记录详细日志，除非进行调试，否则不建议启用dns2tcp_logfile=&#39;/var/log/dns2tcp.log&#39;  # 日志文件，如果不想保存日志可以改为 /dev/null ## iptsipts_if_lo=&#39;lo&#39;                 # 环回接口的名称，在标准发行版中，通常为 lo，如果不是请修改ipts_rt_tab=&#39;233&#39;               # iproute2 路由表名或表 ID，除非产生冲突，否则不建议改动该选项ipts_rt_mark=&#39;0x2333&#39;           # iproute2 策略路由的防火墙标记，除非产生冲突，否则不建议改动该选项ipts_set_snat=&#39;true&#39;           # 设置 iptables 的 MASQUERADE 规则，布尔值，`true/false`，详见 READMEipts_set_snat6=&#39;false&#39;          # 设置 ip6tables 的 MASQUERADE 规则，布尔值，`true/false`，详见 READMEipts_reddns_onstop=&#39;true&#39;       # ss-tproxy stop 后，是否将其它主机发至本机的 DNS 重定向至直连 DNS，详见 READMEipts_proxy_dst_port=&#39;1:65535&#39;   # 黑名单 IP 的哪些端口走代理，多个用逗号隔开，冒号为端口范围(含边界)，详见 README ## optsopts_ss_netstat=&#39;auto&#39;                  # auto/ss/netstat，用哪个端口检测工具，见 READMEopts_ping_cmd_to_use=&#39;auto&#39;             # auto/standalone/parameter，ping 相关，见 READMEopts_hostname_resolver=&#39;auto&#39;           # auto/dig/getent/ping，用哪个解析工具，见 READMEopts_overwrite_resolv=&#39;false&#39;           # true/false/留空，如何操作 resolv.conf，见 READMEopts_ip_for_check_net=&#39;223.5.5.5&#39;       # 检测外网是否可访问的 IP，ping，留空表示跳过此检查 ## filefile_gfwlist_txt=&#39;/etc/ss-tproxy/gfwlist.txt&#39;      # gfwlist/chnlist 模式预置文件file_gfwlist_ext=&#39;/etc/ss-tproxy/gfwlist.ext&#39;      # gfwlist/chnlist 模式扩展文件file_ignlist_ext=&#39;/etc/ss-tproxy/ignlist.ext&#39;      # global/chnroute 模式扩展文件file_chnroute_set=&#39;/etc/ss-tproxy/chnroute.set&#39;    # chnroute 地址段文件 (iptables)file_chnroute6_set=&#39;/etc/ss-tproxy/chnroute6.set&#39;  # chnroute6 地址段文件 (ip6tables)file_dnsserver_pid=&#39;/etc/ss-tproxy/.dnsserver.pid&#39; # dns 服务器进程的 pid 文件 (shell) ## 主要放通下内网的访问，然后把MASQUERADE提前出来，要不然tcp的连接会回不来post_start()&#123;iptables -t nat -I PREROUTING -s 10.207.0.0/16 -d 10.0.0.0/8 -j ACCEPTiptables -t nat -I SSTP_POSTROUTING -s 10.207.0.0/16 -j MASQUERADE&#125; post_stop()&#123;iptables -t nat -D PREROUTING -s 10.207.0.0/16 -d 10.0.0.0/8 -j ACCEPTiptables -t nat -D SSTP_POSTROUTING -s 10.207.0.0/16 -j MASQUERADE&#125;</code></pre><p>以下配置需要特别注意，如果不知道如何配置的，那么就按照我这么默认配置即可<br>具体配置介绍：<a href="https://github.com/zfl9/ss-tproxy#%E9%85%8D%E7%BD%AE%E8%AF%B4%E6%98%8E">https://github.com/zfl9/ss-tproxy#%E9%85%8D%E7%BD%AE%E8%AF%B4%E6%98%8E</a><br><img src="https://raw.githubusercontent.com/almightyYantao/blog-img/master/20230216143840.png"/><br><img src="https://raw.githubusercontent.com/almightyYantao/blog-img/master/20230216142810.png" /></p><h2 id="3、设置ss-libev代理"><a href="#3、设置ss-libev代理" class="headerlink" title="3、设置ss-libev代理"></a>3、设置ss-libev代理</h2><pre><code class="bash">#新版本(v4.``6.1``及以上)#第一次运行时，请执行下面这两个操作#``1``.创建proxy用户和组: useradd -Mr -d/tmp -s/bin/bash proxy#``2``.授予透明代理相关权限: setcap cap_net_bind_service,cap_net_admin+ep /path/to/ss-redir#&gt;&gt; 若setcap不可用，可使用suid权限位，此时需配置：proxy_procuser=``&#39;&#39;``、proxy_procgroup=``&#39;proxy&#39;#&gt;&gt; 将所有者(组)改为root，并授予suid权限：chown root:root /path/to/ss-redir &amp;&amp; chmod` `4755` `/path/to/ss-redirproxy_procuser=``&#39;proxy&#39;#proxy_startcmd=``&#39;su proxy -c&quot;(ss-redir -c /etc/ss.json -u -v &lt;/dev/null &amp;&gt;&gt;/tmp/ss-redir.log &amp;)&quot;&#39;` `# -v 表示记录详细日志proxy_startcmd=``&#39;su proxy -c&quot;(ss-redir -c /etc/ss.json -u &lt;/dev/null &amp;&gt;&gt;/tmp/ss-redir.log &amp;)&quot;&#39;` `# 这里就不记录详细日志了proxy_stopcmd=``&#39;kill -9 $(pidof ss-redir)&#39;</code></pre><h2 id="黑名单、白名单说明"><a href="#黑名单、白名单说明" class="headerlink" title="黑名单、白名单说明"></a>黑名单、白名单说明</h2><ul><li>对于 global 模式，白名单文件为 <code>ignlist.ext</code>，没有黑名单文件，因为默认都走代理。</li><li>对于 gfwlist 模式，黑名单文件为 <code>gfwlist.txt/ext</code>，没有白名单文件，因为其它都走直连。</li><li>对于 chnroute 模式，白名单文件为 <code>ignlist.ext</code>，没有黑名单文件，但允许开启此功能，见下。<br>如果想让 chnroute 模式支持黑名单扩展，请打开 chinadns-ng 的 gfwlist 模式（<code>chinadns_gfwlist_mode</code>）；开启 gfwlist 模式后，chinadns-ng 会读取 <code>gfwlist.txt/ext</code> 黑名单文件中的<strong>域名模式</strong>；当 chinadns-ng 收到域名解析请求时，会先检查给定域名是否在黑名单中，如果是则只向可信 DNS 发出解析请求（也就是 <code>dns_remote/dns_remote6</code>），因此解析出来的会是国外 IP（不一定，具体要看给定域名的A&#x2F;AAAA记录以及其dns解析设定），然后当客户端访问该 IP 时就会走代理出去了（如果解析的地址是国外地址）。</li></ul><blockquote><p><code>chinadns_gfwlist_mode</code>的本意其实并不是为了支持’黑名单’，而是为了提高 chinadns-ng 的准确性，降低 dns 污染的可能性</p></blockquote><p>启动后，ss-tproxy会自动帮你做透明代理，会设置好iptables的配置，不需要在手工配置<br>同时ss-tproxy还提供勾子的形式，来帮助解决分流或者内网访问的一个方案，具体参考：</p><ul><li><a href="https://github.com/zfl9/ss-tproxy#%E9%92%A9%E5%AD%90%E5%87%BD%E6%95%B0">https://github.com/zfl9/ss-tproxy#%E9%92%A9%E5%AD%90%E5%87%BD%E6%95%B0</a></li><li><a href="https://github.com/zfl9/ss-tproxy#%E9%92%A9%E5%AD%90%E5%87%BD%E6%95%B0%E5%B0%8F%E6%8A%80%E5%B7%A7">https://github.com/zfl9/ss-tproxy#%E9%92%A9%E5%AD%90%E5%87%BD%E6%95%B0%E5%B0%8F%E6%8A%80%E5%B7%A7</a></li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> 代理 </tag>
            
            <tag> 透明代理 </tag>
            
            <tag> vpn </tag>
            
            <tag> 流量转发 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>清理自定义DHCP下发，异常断开导致IP地址占用的问题</title>
      <link href="/2023/02/16/openvpn/clearOpenvpnInvalidIp/"/>
      <url>/2023/02/16/openvpn/clearOpenvpnInvalidIp/</url>
      
        <content type="html"><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>因为OpenVpn存在断开重连的机制，如果突然出现网络抖动，用户端没有触发断开命令，但是同时又触发了重新连接的命令，这时候VPN这里就会重新给下发一个新的地址，但是老的地址不会回收掉，这个也是很早之前DHCP被用完的真正原因</p><h1 id="处理方式"><a href="#处理方式" class="headerlink" title="处理方式"></a>处理方式</h1><p>在每一台openvpn的机器上新增一个shell脚本做定时任务，每天临晨2点开始循环遍历</p><pre><code class="bash">#!/bin/bashIFS=$&#39;\n&#39; # 修改默认分隔符OLDIFS=&quot;$IFS&quot; # 清理无效的IP地址function clearIp()&#123;    # 取出当前已经分配下去的所有IP地址    result=$(curl -s &quot;后端获取当前使用IP的接口地址&quot;);    ipList=`echo $result | jq &#39;.d.result&#39;`;    ipListLength=`echo $ipList | jq &#39;.|length&#39;`;    date=`date &quot;+%Y-%m-%d %H:%M:%S&quot;`     # 循环所有的数据    for index in `seq 0 $ipListLength`    do        ip=`echo $ipList | jq -r &quot;.[$index].ip&quot;`;        common_name=`echo $ipList | jq -r &quot;.[$index].user&quot;`        # 拿到当前真正在使用的IP地址        useIp=`cat /var/log/openvpn/status.log | grep CLIENT_LIST | awk &#39;&#123;if (NR&gt;1)&#123;print $1&#125;&#125;&#39; | cut -d &#39;,&#39; -f 4`        # 如果当前IP已经不在使用了，那么就需要释放，防止后期地址池不够用的情况        if [[ &quot;$&#123;useIp[@]&#125;&quot;  =~ &quot;$&#123;ip&#125;&quot; ]]; then            echo &quot;$&#123;date&#125; | $&#123;ip&#125; | 当前IP属于使用状态，无需释放&quot;;        else            curl -s &quot;释放IP的接口地址&quot; &gt;&gt; /var/log/openvpn/disconnect.log            echo &quot;$&#123;date&#125; | $&#123;ip&#125; | $&#123;common_name&#125; | 当前IP需要释放&quot;        fi    done&#125; # 清理Iptable的规则function clearIptable()&#123;    iptableResult=($(iptables -L | grep 10.207 | grep ACCEPT | awk &#39;&#123;print $7&#125;&#39;))    iptableResultCount=($(iptables -L | grep 10.207 | grep ACCEPT | awk &#39;&#123;print $7&#125;&#39; | wc -l))    date=`date &quot;+%Y-%m-%d %H:%M:%S&quot;`    for item in $&#123;iptableResult[@]&#125;    do        ip=`echo $item | cut -d &#39;-&#39; -f 2`        common_name=`echo $item | cut -d &#39;-&#39; -f 1`        ifconfig_pool_remote_ip=$ip        useIp=`cat /var/log/openvpn/status.log | grep CLIENT_LIST | awk &#39;&#123;if (NR&gt;1)&#123;print $1&#125;&#125;&#39; | cut -d &#39;,&#39; -f 4`        # 如果当前IP已经不在使用了，那么就需要释放，防止后期地址池不够用的情况        if [[ &quot;$&#123;useIp[@]&#125;&quot;  =~ &quot;$&#123;ip&#125;&quot; ]]; then            echo &quot;$&#123;date&#125; | $&#123;ip&#125; | 当前IP属于使用状态，无需释放&quot;;        else            echo &quot;$&#123;date&#125; | $&#123;ifconfig_pool_remote_ip&#125; | $&#123;common_name&#125; | 当前IP需要释放&quot;            /sbin/iptables -D FORWARD -s $ifconfig_pool_remote_ip -m set --match-set $&#123;common_name&#125;-$&#123;ifconfig_pool_remote_ip&#125; dst -j ACCEPT            /sbin/iptables -D FORWARD -s $ifconfig_pool_remote_ip -m set --match-set $&#123;common_name&#125;-$&#123;ifconfig_pool_remote_ip&#125;-drop dst -j DROP            /sbin/iptables -D FORWARD -s $ifconfig_pool_remote_ip -j DROP            /sbin/ipset destroy $&#123;common_name&#125;-$&#123;ifconfig_pool_remote_ip&#125;            /sbin/ipset destroy $&#123;common_name&#125;-$&#123;ifconfig_pool_remote_ip&#125;-drop        fi    done&#125; clearIp()clearIptable()</code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> openvpn </tag>
            
            <tag> dhcp </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>openvpn动态下发权限</title>
      <link href="/2022/12/14/openvpn/openvpn-dynamic-route-permissions/"/>
      <url>/2022/12/14/openvpn/openvpn-dynamic-route-permissions/</url>
      
        <content type="html"><![CDATA[<h2 id="首先了解我们为什么要动态下发"><a href="#首先了解我们为什么要动态下发" class="headerlink" title="首先了解我们为什么要动态下发"></a>首先了解我们为什么要动态下发</h2><ul><li>开源的openvpn并不支持权限管理，大部分在做权限管理的时候使用的都是根据来源IP或IP段通过iptables&#x2F;交换机来进行权限控制</li><li>权限控制太广了，根本无法很好的去做管理后台的配置，特别是需要用户组来进行区分的时候，那就更困难了</li></ul><h2 id="权限下发的逻辑"><a href="#权限下发的逻辑" class="headerlink" title="权限下发的逻辑"></a>权限下发的逻辑</h2><img  src=http://www.plantuml.com/plantuml/svg/Kr3WAiaioKbLo2z8pIiho7knVzwplgNFb-nwrjB_TFO-hkTJLZphsAwWQmTJOFMoOTQB_UrFMzSytpiHeXegmmMbTUrWIf_iw7xQFz4puEcS3K_dDhpVq_DYtRxdUzSzxLhnhA7nsTpcbpCd83H1RB9wkgxdMSS4YC40><h2 id="用到的技术"><a href="#用到的技术" class="headerlink" title="用到的技术"></a>用到的技术</h2><ul><li>ipset</li><li>iptables</li></ul><h2 id="主要脚本内容"><a href="#主要脚本内容" class="headerlink" title="主要脚本内容"></a>主要脚本内容</h2><h3 id="连接脚本"><a href="#连接脚本" class="headerlink" title="连接脚本"></a>连接脚本</h3><p>&#96;&#96;&#96;bash<br>&#x2F;sbin&#x2F;ipset create ${common_name}-${common_ip} hash:ip<br>&#x2F;sbin&#x2F;ipset create ${common_name}-${common_ip}-drop hash:ip</p><h1 id="这里是动态去你的后端获取出来的允许访问的列表，接口自己去实现"><a href="#这里是动态去你的后端获取出来的允许访问的列表，接口自己去实现" class="headerlink" title="这里是动态去你的后端获取出来的允许访问的列表，接口自己去实现"></a>这里是动态去你的后端获取出来的允许访问的列表，接口自己去实现</h1><p>for ((i &#x3D; 0; i &lt; $</p>]]></content>
      
      
      
        <tags>
            
            <tag> openvpn </tag>
            
            <tag> 权限 </tag>
            
            <tag> 动态下发 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>zabbix 警报推送至企业微信（图文版）</title>
      <link href="/2022/12/13/zabbix/zabbix-wecom/"/>
      <url>/2022/12/13/zabbix/zabbix-wecom/</url>
      
        <content type="html"><![CDATA[<h2 id="新增Python脚本"><a href="#新增Python脚本" class="headerlink" title="新增Python脚本"></a>新增Python脚本</h2><pre><code class="python"># encoding: utf-8import sysimport requestsimport jsonimport osimport timeimport re url = &#39;http://10.1.1.59/zabbix/api_jsonrpc.php&#39;headers = &#123;&#39;Content-Type&#39;: &#39;application/json-rpc&#39;&#125;graph_path = &#39;/data/zabbix/images/&#39;  # 定义图片存储路径graph_url = &#39;http://10.1.1.59/zabbix/chart.php&#39;  # 定义图表的urlloginurl = &quot;http://10.1.1.59/zabbix/index.php&quot;  # 定义登录的url  def uploadImg(path,accessToken):    #img_url = &quot;https://qyapi.weixin.qq.com/cgi-bin/webhook/upload_media?key=&quot; + key + &quot;&amp;type=file&quot;    img_url = &quot;https://qyapi.weixin.qq.com/cgi-bin/media/uploadimg?access_token=&quot;+accessToken    files = &#123;&#39;media&#39;: open(path, &#39;rb&#39;)&#125;    r = requests.post(img_url, files=files)    re = json.loads(r.text)    print(re)    return re[&#39;url&#39;]  def get_itemid(message):    #print(message)    itemid = re.search(r&#39;ITEMID:(\d+)&#39;, message).group(1)    #itemid = 1    return itemid  def get_imgUrl(itemid):    session = requests.Session()    try:        loginheaders = &#123;            &quot;Host&quot;: &quot;10.1.1.59&quot;,            &quot;Accept&quot;: &quot;text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.9&quot;        &#125;        # 定义请求消息头         payload = &#123;            &quot;name&quot;: &#39;yantao&#39;,            &quot;password&quot;: &#39;xxxxxxx&#39;,            &quot;autologin&quot;: &quot;1&quot;,            &quot;enter&quot;: &quot;Sign in&quot;,        &#125;        # 定义传入的data        login = session.post(url=loginurl, headers=loginheaders, data=payload)        print(login)        graph_params = &#123;            &quot;from&quot;: &quot;now-10m&quot;,            &quot;to&quot;: &quot;now&quot;,            &quot;itemids&quot;: itemid,            &quot;width&quot;: &quot;400&quot;,        &#125;        # 定义获取图片的参数        graph_req = session.get(url=graph_url, params=graph_params)        # 发送get请求获取图片数据        time_tag = time.strftime(&quot;%Y%m%d%H%M%S&quot;, time.localtime())        graph_name = &#39;baojing_&#39; + time_tag + &#39;.png&#39;        # 用报警时间来作为图片名进行保存        graph_name = os.path.join(graph_path, graph_name)        # 使用绝对路径保存图片        with open(graph_name, &#39;wb&#39;, ) as f:            f.write(graph_req.content)            # 将获取到的图片数据写入到文件中去        return graph_name    except Exception as e:        print(e)        return False def getAccessToken():    api_url = &quot;https://qyapi.weixin.qq.com/cgi-bin/gettoken?corpid=corpid&amp;corpsecret=corpsecret&quot;    content = requests.get(api_url)    #print(content.json())    return content.json().get(&quot;access_token&quot;) def getImgUrl(mediaId,accessToken):    api_url = &quot;https://qyapi.weixin.qq.com/cgi-bin/media/get?access_token=&quot;+accessToken+&quot;&amp;media_id=&quot;+mediaId    content = requests.get(api_url)    print(mediaId)    print(content.json())    return content.json().get(&quot;url&quot;) def send_message(imgUrl,title,desc,openUrl,key):    # 发送消息    url = &quot;https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=&quot; + key    # message = title  # sys.argv[3]    params = &#123;        &quot;msgtype&quot;: &quot;template_card&quot;,        &quot;template_card&quot;: &#123;            &quot;card_type&quot;: &quot;news_notice&quot;,            &quot;source&quot;: &#123;                &quot;desc&quot;: &quot;Zabbix网络警报&quot;,                &quot;desc_color&quot;: 0            &#125;,            &quot;main_title&quot;:&#123;                &quot;title&quot;:&quot;Zabbix网络警报&quot;,            &#125;,            &quot;quote_area&quot;:&#123;                &quot;type&quot;:0,                &quot;quote_text&quot;:desc            &#125;,            &quot;card_image&quot;: &#123;                &quot;url&quot;: imgUrl            &#125;,            &quot;card_action&quot;: &#123;                &quot;type&quot;: 1,                &quot;url&quot;: openUrl,                &quot;appid&quot;: &quot;APPID&quot;,                &quot;pagepath&quot;: &quot;PAGEPATH&quot;            &#125;        &#125;    &#125;    req = requests.post(url, data=json.dumps(params))    print(req.json())  if __name__ == &#39;__main__&#39;:    message = sys.argv[1]    print(message)    itemid = get_itemid(message)    imgpath = get_imgUrl(itemid)    accessToken = getAccessToken();    imgUrl = uploadImg(imgpath,accessToken)    #print(itemid)    #print(imgpath)    print(imgUrl)    send_message(imgUrl,sys.argv[2],sys.argv[3],imgUrl,sys.argv[4])    #accessToken = getAccessToken()</code></pre><h2 id="新增SH脚本"><a href="#新增SH脚本" class="headerlink" title="新增SH脚本"></a>新增SH脚本</h2><pre><code class="shell">#!/bin/bashecho $1 &gt;&gt; /data/zabbix/log.logpython /usr/lib/zabbix/alertscripts/wxcom.py $1 $2 $3 $4</code></pre><p>把两个文件都放到这个目录下：&#x2F;usr&#x2F;lib&#x2F;zabbix&#x2F;alertscripts&#x2F;</p><h2 id="配置媒介"><a href="#配置媒介" class="headerlink" title="配置媒介"></a>配置媒介</h2>]]></content>
      
      
      
        <tags>
            
            <tag> zabbix </tag>
            
            <tag> 企业微信 </tag>
            
            <tag> 报警 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>安装&amp;破解Openvpn Access Server</title>
      <link href="/2022/12/13/openvpn/openvpn-access-server/"/>
      <url>/2022/12/13/openvpn/openvpn-access-server/</url>
      
        <content type="html"><![CDATA[<h2 id="在线安装（需要翻墙）"><a href="#在线安装（需要翻墙）" class="headerlink" title="在线安装（需要翻墙）"></a>在线安装（需要翻墙）</h2><pre><code class="shell">yum -y install https://as-repository.openvpn.net/as-repo-centos7.rpmyum -y install openvpn-as</code></pre><h2 id="管理员登录"><a href="#管理员登录" class="headerlink" title="管理员登录"></a>管理员登录</h2><p>首先需要修改管理员密码</p><pre><code class="shell">passwd openvpn</code></pre><p>输入之后可以登录管理员页面了。</p><p>如果密码一直错误的话，说明修改失败了，可以查看下原来的密码是什么</p><pre><code class="shell">cat /usr/local/openvpn_as/init.log</code></pre><p>登录后点击User Management–&gt;User Permissions 添加用户</p><h2 id="破解用户数"><a href="#破解用户数" class="headerlink" title="破解用户数"></a>破解用户数</h2><ul><li>主要操作的文件是一个名叫 <code>pyovpn-2.0-pyx.x.egg</code> 的文件，以我了解的情况来看，从 <code>2.5.0</code> 到 <code>2.9.x</code> 文件名一直都是这个，只是不同版本里面的内容不一样.</li><li>这个文件有点类似 <code>Java</code> 当中的 <code>jar</code> 库文件，也是一个 <code>zip</code> 压缩文件，里面包含了一些 <code>Python</code> 的字节码文件.</li><li>破解的原理大概是在 <code>Python</code> 中采用类似 <code>Java</code> 动态代理的技术，将原本读取用户属性的调用返回值拦截，修改用户限制数量再返回.</li></ul><h3 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h3><p><code>2.9.0</code> 以下版本破解的目标文件是 <code>/pyovpn/lic/uprop.pyo</code>, <code>2.9.0</code> 及以上是 <code>/pyovpn/lic/uprop.pyc</code>; 按照网上流行的破解方法，把这个文件解压出来并改名为 <code>uprop2.pyo</code> 或 <code>uprop2.pyc</code>, 然后新建一个 <code>uprop.py</code> 文件，内容如下:</p><p><code>**2.9.0**</code><strong>以下版本内容:</strong></p><pre><code class="shell">import uprop2old_figure = Nonedef new_figure(self, licdict):    ret = old_figure(self, licdict)    ret[&#39;concurrent_connections&#39;] = 1024    return retfor x in dir(uprop2):    if x[:2] == &#39;__&#39;:        continue    if x == &#39;UsageProperties&#39;:        exec(&#39;old_figure = uprop2.UsageProperties.figure&#39;)        exec(&#39;uprop2.UsageProperties.figure = new_figure&#39;)    exec(&#39;%s = uprop2.%s&#39; % (x, x))</code></pre><p><code>**2.9.0**</code><strong>及以上版本内容:</strong></p><pre><code class="shell">from pyovpn.lic import uprop2old_figure = Nonedef new_figure(self, licdict):    ret = old_figure(self, licdict)    ret[&#39;concurrent_connections&#39;] = 1024    return retfor x in dir(uprop2):    if x[:2] == &#39;__&#39;:        continue    if x == &#39;UsageProperties&#39;:        exec(&#39;old_figure = uprop2.UsageProperties.figure&#39;)        exec(&#39;uprop2.UsageProperties.figure = new_figure&#39;)    exec(&#39;%s = uprop2.%s&#39; % (x, x))</code></pre><p>再将上面的 <code>uprop.py</code> 编译为库文件 <code>uprop.pyo</code> 或 <code>uprop.pyc</code>:</p><pre><code class="shell"># &lt;2.9.0python2 -O -m compileall uprop.py# &gt;=2.9.0python3 -O -m compileall uprop.py &amp;&amp; mv __pycache__/uprop.cpython-37.opt-1.pyc uprop.pyc</code></pre><blockquote><p>注意 <code>uprop.cpython-37.opt-1.pyc</code> 文件名会随着 <code>python</code> 版本变化而变化.</p></blockquote><p>现在我们得到了一个改文件名的文件 <code>uprop2.pyo</code> 或 <code>uprop2.pyc</code>, 和一个编译出来的 <code>uprop.pyo</code> 或 <code>uprop.pyc</code>; 把这两个文件压缩到 <code>pyovpn-2.0-pyx.x.egg</code> 的 <code>/pyovpn/lic/</code> 目录下，然后去服务器替换目标文件，重启服务就 OK 了.</p><h2 id="所有的操作命令"><a href="#所有的操作命令" class="headerlink" title="所有的操作命令"></a>所有的操作命令</h2><pre><code class="shell"># 把操作的文件复制出来到另外的一个文件夹下面操作cd /usr/local/openvpn_as/lib/python &amp; mkdir pojie &amp; cp python/pyovpn-2.0-py3.6.egg ./pojie &amp; cd pojie/# 改一个文件名，防止后面打包的时候把源文件覆盖了mv pyovpn-2.0-py3.6.egg pyovpn-2.0-py3.6.egg.bak &amp; unzip pyovpn-2.0-py3.6.egg.bak# 进入到需要修改的目录cd pyovpn/lic# 复制源文件改名mv uprop.pyc uprop2.pyc# 新增一个py文件，然后把文件内容复制进去，并保存vi uprop.py# 退到 pojie 文件夹，进行打包zip -r pyovpn-2.0-py3.6.egg EGG-INFO/ common/ pyovpn/# 复制文件到运行目录，覆盖cp pyovpn-2.0-py3.6.egg /usr/local/openvpn_as/lib/python# 重启服务systemctl restart openvpnas</code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> openvpn </tag>
            
            <tag> 2.9.0 </tag>
            
            <tag> openvpn access server </tag>
            
            <tag> 破解 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>通过 Haproxy 实现 Shadowshadows 负载均衡</title>
      <link href="/2022/12/13/haproxy-load/"/>
      <url>/2022/12/13/haproxy-load/</url>
      
        <content type="html"><![CDATA[<h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>缺点：所有的SS的加密方式和密码必须一致<br>介绍：HAProxy是一个使用C语言编写的自由及开放原始码软件，其提供高可用性、负载均衡，以及基于TCP和HTTP的应用程序代理。</p><h2 id="安装Haproxy"><a href="#安装Haproxy" class="headerlink" title="安装Haproxy"></a>安装Haproxy</h2><pre><code class="shell">yum install haproxy</code></pre><h2 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h2><pre><code class="shell">global    chroot  /var/lib/haproxy    pidfile /var/run/haproxy.pid    user    haproxy    group   haproxydefaults    mode    tcp                         #服务器默认的工作模式    balance roundrobin                  #服务器默认使用的均衡模式    retries 3                           #三次连接失败表示服务器不可用    maxconn 5000                        #最大连接数    timeout connect 500ms               #连接超时    timeout client  3s                  #客户端超时    timeout server  3s                  #服务器超时listen WebPanel    mode    http                        #这里使用HTTP模式    bind    10.1.1.58:9595               #WEB服务端口    stats   refresh 5s                  #自动刷新时间    stats   uri  /                      #WEB管理地址    stats   auth admin:xxxx         #账号密码    stats   hide-version                #隐藏版本    stats   admin if TRUE               #验证通过则赋予管理权frontend shadowsocks-in    bind *:50001    default_backend serversbackend servers    mode tcp    balance roundrobin    # option external-check    option tcp-check    # external-check command &quot;/etc/haproxy/haproxy-shadowsocks-checker.py&quot;    server ss1 43.156.100.84:2195 check inter 500 rise 2 fall 4 weight 100   #SS/SSR服务器地址与端口    server ss2 42.157.192.42:10108 check inter 500 rise 2 fall 4 weight 100</code></pre><p><code>server</code>：后面首先跟名字，名字随便起呗，自己能够区分就行。紧接着跟SS的公网IP+端口，端口也就是SS&#x2F;SSR的端口。<br><code>check</code>：是检测的意思，这段配置很重要<br><code>inter</code>：单位毫秒，我配置的500，即500毫秒检测一次目标服务器。<br><code>rise2</code>：设定健康状态检查中，某离线的服务器从离线状态转换至正常状态需要成功检查的次数，这里我设置的2次。<br><code>fall4</code>：确认服务器从正常状态转换为不可用状态需要检查的次数，这里是4次。<br><code>weight</code>：权重，值越大代表这台机器工作的机会越多，这里我们可以把一台线路较好的机器的权重设置高一些。<br><code>balance</code>：负载均衡方式</p><ul><li>roundrobin：基于权重进行轮叫，在服务器的处理时间保持均匀分布时，这是最平衡、最公平的算法。此算法是动态的，这表示其权重可以在运行时进行调整，不过，在设计上，每个后端服务器仅能最多接受4128个连接；</li><li>static-rr：基于权重进行轮叫，与roundrobin类似，但是为静态方法，在运行时调整其服务器权重不会生效；不过，其在后端服务器连接数上没有限制；</li><li>leastconn：新的连接请求被派发至具有最少连接数目的后端服务器；在有着较长时间会话的场景中推荐使用此算法，如LDAP、SQL等，其并不太适用于较短会话的应用层协议，如HTTP；此算法是动态的，可以在运行时调整其权重；</li><li>source：将请求的源地址进行hash运算，并由后端服务器的权重总数相除后派发至某匹配的服务器；这可以使得同一个客户端IP的请求始终被派发至某特定的服务器；不过，当服务器权重总数发生变化时，如某服务器宕机或添加了新的服务器，许多客户端的请求可能会被派发至与此前请求不同的服务器；常用于 负载均衡无cookie功能的基于TCP的协议；其默认为静态，不过也可以使用hash-type修改此特性；</li></ul><h2 id="修改Shadowsocks配置"><a href="#修改Shadowsocks配置" class="headerlink" title="修改Shadowsocks配置"></a>修改Shadowsocks配置</h2><pre><code class="shell">&#123;    &quot;server&quot;:&quot;127.0.0.1&quot;, # 这里改成Haproxy的地址    &quot;server_port&quot;:50001, # 这里改成Haproxy监听的端口    &quot;local_port&quot;:1080, # 这里本地监听端口    &quot;password&quot;:&quot;qunheadmin&quot;,    &quot;timeout&quot;:60,    &quot;method&quot;:&quot;aes-256-gcm&quot;,    &quot;mode&quot;:&quot;tcp_and_udp&quot;&#125;</code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>openvpn-auth（支持企业微信认证&amp;LDAP）</title>
      <link href="/2022/12/13/openvpn/openvpn-auth/"/>
      <url>/2022/12/13/openvpn/openvpn-auth/</url>
      
        <content type="html"><![CDATA[<h2 id="方案介绍"><a href="#方案介绍" class="headerlink" title="方案介绍"></a>方案介绍</h2><h3 id="时序图"><a href="#时序图" class="headerlink" title="时序图"></a>时序图</h3><img src="/2022/12/13/openvpn/openvpn-auth/image2022-12-12_20-11-38.png" class="" alt="openvpn-auth 时序图"><h2 id="其中有两个地方需要修改"><a href="#其中有两个地方需要修改" class="headerlink" title="其中有两个地方需要修改"></a>其中有两个地方需要修改</h2><ul><li><code>corpid</code>: 企业微信的企业ID</li><li><code>corpsecret</code>: 拥有通讯录的企业微信的Secret</li></ul><h2 id="代码如下"><a href="#代码如下" class="headerlink" title="代码如下"></a>代码如下</h2><pre><code class="go">package authimport (    &quot;io/ioutil&quot;    &quot;log&quot;    &quot;net/http&quot;    &quot;strconv&quot;    &quot;github.com/tidwall/gjson&quot;)func GetAccessToken() string &#123;    return GetWecomToken()&#125;// 从企业微信获取Tokenfunc GetWecomToken() string &#123;    //创建一个请求    req, err := http.NewRequest(&quot;GET&quot;, &quot;https://qyapi.weixin.qq.com/cgi-bin/gettoken?corpid=corpid&amp;corpsecret=corpsecret&quot;, nil)    if err != nil &#123;        log.Println(&quot;NewRquest:&quot;, err)    &#125;    client := &amp;http.Client&#123;&#125;    //设置请求头    req.Header.Set(&quot;Content-Type&quot;, &quot;application/json; charset=utf-8&quot;)    //发送请求    resp, err := client.Do(req)    //关闭请求    defer resp.Body.Close()    body, err := ioutil.ReadAll(resp.Body)    access_token := gjson.Get(string(body), &quot;access_token&quot;)    return access_token.Str&#125;// 获取员工信息func WeComAuth(userId string) bool &#123;    //创建一个请求    req, err := http.NewRequest(&quot;GET&quot;, `https://qyapi.weixin.qq.com/cgi-bin/user/get?access_token=`+GetAccessToken()+`&amp;userid=`+userId, nil)    if err != nil &#123;        log.Println(&quot;NewRquest:&quot;, err)    &#125;    client := &amp;http.Client&#123;&#125;    //设置请求头    req.Header.Set(&quot;Content-Type&quot;, &quot;application/json; charset=utf-8&quot;)    //发送请求    resp, err := client.Do(req)    //关闭请求    defer resp.Body.Close()    body, err := ioutil.ReadAll(resp.Body)    errcode := gjson.Get(string(body), &quot;errcode&quot;)    int, err := strconv.Atoi(errcode.Str)    return int == 0&#125;</code></pre><h2 id="使用方式"><a href="#使用方式" class="headerlink" title="使用方式"></a>使用方式</h2><p>修改<code>/etc/openvpn/server.conf</code>的验证方式，可以吧其他的验证去掉</p><pre><code class="shell">auth-user-pass-verify /etc/openvpn/openvpn-wecom-password via-file</code></pre><h2 id="加解密方式"><a href="#加解密方式" class="headerlink" title="加解密方式"></a>加解密方式</h2><p>因为用到了客户端传过来的时候的加密方式，因此两边的加密方式必须一致</p><pre><code class="go">package utilimport (    &quot;bytes&quot;    basicAES &quot;crypto/aes&quot;    &quot;crypto/cipher&quot;    &quot;encoding/base64&quot;)type Aes struct &#123;    securityKey []byte    iv          []byte&#125;/** * constructor */func AesTool() *Aes &#123;    return &amp;Aes&#123;[]byte(&quot;123&quot;), []byte(&quot;123&quot;)&#125;&#125;/** * 加密 * @param string $plainText 明文 * @return bool|string */func (a Aes) Encrypt(plainText string) (string, error) &#123;    block, err := basicAES.NewCipher(a.securityKey)    if err != nil &#123;        return &quot;&quot;, err    &#125;    plainTextByte := []byte(plainText)    blockSize := block.BlockSize()    plainTextByte = AddPKCS7Padding(plainTextByte, blockSize)    cipherText := make([]byte, len(plainTextByte))    mode := cipher.NewCBCEncrypter(block, a.iv)    mode.CryptBlocks(cipherText, plainTextByte)    return base64.StdEncoding.EncodeToString(cipherText), nil&#125;/** * 解密 * @param string $cipherText 密文 * @return bool|string */func (a Aes) Decrypt(cipherText string) (string, error) &#123;    block, err := basicAES.NewCipher(a.securityKey)    if err != nil &#123;        return &quot;&quot;, err    &#125;    cipherDecodeText, decodeErr := base64.StdEncoding.DecodeString(cipherText)    if decodeErr != nil &#123;        return &quot;&quot;, decodeErr    &#125;    mode := cipher.NewCBCDecrypter(block, a.iv)    originCipherText := make([]byte, len(cipherDecodeText))    mode.CryptBlocks(originCipherText, cipherDecodeText)    originCipherText = StripPKSC7Padding(originCipherText)    return string(originCipherText), nil&#125;/** * 填充算法 * @param string $source * @return string */func AddPKCS7Padding(ciphertext []byte, blockSize int) []byte &#123;    padding := blockSize - len(ciphertext)%blockSize    paddingText := bytes.Repeat([]byte&#123;byte(padding)&#125;, padding)    return append(ciphertext, paddingText...)&#125;/** * 移去填充算法 * @param string $source * @return string */func StripPKSC7Padding(cipherText []byte) []byte &#123;    length := len(cipherText)    unpadding := int(cipherText[length-1])    return cipherText[:(length - unpadding)]&#125;</code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> 企业微信 </tag>
            
            <tag> openvpn </tag>
            
            <tag> openvpn-auth </tag>
            
            <tag> wecom </tag>
            
            <tag> 认证 </tag>
            
            <tag> ldap </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
